# configs

Configuration files for selection, training, evaluation, models, and environment-specific runs.

Rule: configuration changes must be versioned. Do not silently overwrite old config files used in previous runs.
