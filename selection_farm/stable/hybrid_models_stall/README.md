# hybrid_models_stall

Stores hybrid pipelines combining ML + LLM + embedding components.

Expected structure per hybrid:

```
<hybrid_name>/
├── pipeline.yaml
├── components.yaml
├── integration_notes.md
├── eval_report.json
└── status.yaml
```
