# scripts

Operational helper scripts. Must be small, explicit, and safe.

Rule: any destructive script must require explicit confirmation.

`generate_id.py` issues Selection Farm IDs (models, runs, datasets, etc.) — see [configs/id_mapping/README.md](../configs/id_mapping/README.md) for the ID format and rules.
